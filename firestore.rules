rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Backend Admin SDK bypasses these rules. Client SDK has zero access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
